import { HoraNumericStringPipe } from './hora-numeric-string.pipe';

describe('HoraNumericStringPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraNumericStringPipe();
    expect(pipe).toBeTruthy();
  });
});
