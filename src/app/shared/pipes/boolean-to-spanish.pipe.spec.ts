import { BooleanToSpanishPipe } from './boolean-to-spanish.pipe';

describe('BooleanToSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
