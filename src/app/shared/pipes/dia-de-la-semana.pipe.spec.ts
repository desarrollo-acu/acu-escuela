import { DiaDeLaSemanaPipe } from './dia-de-la-semana.pipe';

describe('DiaDeLaSemanaPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaDeLaSemanaPipe();
    expect(pipe).toBeTruthy();
  });
});
