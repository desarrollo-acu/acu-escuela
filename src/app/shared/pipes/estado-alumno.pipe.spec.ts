import { EstadoAlumnoPipe } from './estado-alumno.pipe';

describe('EstadoAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
