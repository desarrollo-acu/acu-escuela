import { FechaStringPipe } from './fecha-string.pipe';

describe('FechaStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaStringPipe();
    expect(pipe).toBeTruthy();
  });
});
