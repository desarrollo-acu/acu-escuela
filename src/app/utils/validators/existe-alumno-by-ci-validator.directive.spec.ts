import { ExisteAlumnoByCiValidatorDirective } from './existe-alumno-by-ci-validator.directive';

describe('ExisteAlumnoByCiValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExisteAlumnoByCiValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
